/**
 * Refresh token
 * 
 * @export
 * @class RefreshToken
 */
// Not in use?
export class RefreshToken {
    token: string;
	scope : string;
	client : string;
	username: string;
	clientId: string;
	authorizationCode : string
}